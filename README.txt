First of all create tg.txt file where will'be contained following info:

TG_TOKEN=YOUR_TG_TOKEN of your created bot (API)
TG_CHAT_ID=YOUR_CHAT_ID (u can get from father bot via TG)
POLL_INTERVAL_SEC=60
FILTER_MARKETS=KRW,USDT

Note, u need to create your first at first to launch the script.